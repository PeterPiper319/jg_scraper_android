Field Ingestion & Winning Source MappingTarget Database FieldWinning Source FileEngineering Rationaletender_id / idmanifest.json (158315)  This is your internal database unique identifier or the portal's system key. Keep it as your primary relational key.  tender_reference_numberenriched.json (value of reference number)  The portal manifest occasionally truncates characters or leaves out symbols. The document extraction pulls the exact legally binding string.  titlemanifest.json  The portal title is often optimized for standard indexing. However, keep the tender_metadata_tender_title from the document as an internal display alternative if the manifest title contains typos.  closing_date_timeBoth (Dual-Storage Rule)Critical Step: Store portal_closing_date and extracted_closing_date separately. If the portal says June 12 but the PDF says June 25, your system must instantly trigger a REGULATORY_ANOMALY flag because a mismatch can disqualify a user or lead to legal disputes.  contact_detailsMerged Array (Unique Union)Deeply combine both blocks. Take the parsed structural keys (town, streetname) from the manifest, but append the deep text-extracted emails and phone numbers from the enriched file. Run a list(set()) union over the arrays to ensure no duplicate contact entries leak onto your PWA.  compliance_requirementsenriched.json  Drop the simple string arrays from the manifest (complianceRequirements). Replace them completely with the explicitly boolean and verified fields from your document schema (sbd_forms, mbd_forms, csd_registration_required).  